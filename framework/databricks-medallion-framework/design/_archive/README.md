# Archived / stale design artifacts

Files here are **historical** and are **not** used by the runtime framework.

| File | Note |
|------|------|
| `STALE_apply_config.py` | Old design sketch. **Use** `src/jobs/apply_control_config.py` instead. |

Do not wire DABs or jobs to anything under `_archive/`.
