entity_key: hr_employees
version: 1
schema:
  columns:
    - name: employee_id
      type: string
      primary_key: true
    - name: email
      type: string
    - name: salary
      type: decimal
business_rules:
  - "email must be present and will be hashed in Silver"
  - "salary must be positive"
